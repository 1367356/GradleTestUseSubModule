package com.li;

/**
 * 把数组排成最小的数。
 */
public class Question33 {
    public int maxNumberLength=10;
    char[] strCombine1 = new char[maxNumberLength * 2 + 1];
    char[] strCombine2 = new char[maxNumberLength * 2 + 1];

    public void pringMinNumber(int[] numbers, int length) {
        if (numbers == null || length <= 0) {
            return;
        }
        char[][] strNumbers = new char[length][];
    }

}
